function scrollToSectionBio() {
  // Rola a página até a seção específica
  var dados = document.getElementById('secaoDadosBio');
  
  if (dados) {
    secaoDadosBio.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToSectionAgenda() {
  // Rola a página até a seção específica
  var dados = document.getElementById('secaoDadosAgenda');
  
  if (dados) {
    secaoDadosAgenda.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToSectionPhoto() {
  var dados = document.getElementById('secaoDadosPhoto');
  
  if (dados) {
    secaoDadosPhoto.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToSectionVideo() {
  var dados = document.getElementById('secaoDadosVideo');
  
  if (dados) {
    secaoDadosVideo.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToSectionDiscography() {
  var dados = document.getElementById('secaoDadosDiscography');
  
  if (dados) {
    secaoDadosDiscography.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToSectionContact() {
  var dados = document.getElementById('secaoDadosContact');
  
  if (dados) {
    secaoDadosContact.scrollIntoView({ behavior: 'smooth' });
  }
}